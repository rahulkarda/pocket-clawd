# The Anthropic API key is stored in macOS Keychain via the in-app Settings UI,
# not in a .env file. This .env.example exists only so contributors know there
# are no plaintext secrets to copy.
#
# Set DEBUG=1 in your shell before `npm run dev` to enable verbose main-process logs.
DEBUG=
